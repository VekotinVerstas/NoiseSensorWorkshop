#include <WiFi101.h>
char ssid[] = "network name";     // WLAN SSID
char pass[] = "passkey";          // WLAN password
IPAddress server(93,184,216,34);  // ip address matching the host header (example.org)
char host[] = "example.org";       // Host header value
char path[] = "/";                // GET path

